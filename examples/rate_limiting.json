{
  "rateLimiting": {
    "enabled": true,
    "strategy": "token-bucket",
    "rules": [
      {
        "endpoint": "/user/*",
        "requestsPerHour": 5000,
        "burstCapacity": 100
      }
    ]
  }
}
